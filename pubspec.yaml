name: famille_budget
description: Famille Budget - suivi des revenus et dépenses de la famille (hors ligne).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  sqflite: ^2.4.1
  path: ^1.9.0
  path_provider: ^2.1.5
  fl_chart: ^0.69.2
  share_plus: ^10.1.4
  file_selector: ^1.0.3
  uuid: ^4.5.1

flutter:
  uses-material-design: true
